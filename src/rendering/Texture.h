#ifndef TEXTURE_H
#define TEXTURE_H

#include <GL/glew.h>
#include <map>
#include <string>

namespace NNY{
    namespace Render{


        struct Texture{
            GLuint texture;
        };

    }
}
#endif /* TEXTURE_H */
